'use client';

// Firebase is no longer used for authentication, so this file can be simplified or removed.
// We will keep a placeholder structure in case other Firebase services are added later.

export const firebaseConfigured = false;
